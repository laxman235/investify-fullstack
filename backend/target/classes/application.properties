# Use custom naming strategy to avoid reserved SQL identifiers (USER, TRANSACTION, ...)
spring.jpa.properties.hibernate.physical_naming_strategy=com.investify.config.PrefixedPhysicalNamingStrategy

# dev-friendly defaults (optional)
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:h2:mem:investify;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Gemini API key (dev only). Prefer using env var in production.
gemini.api.key=AIzaSyBOelBIN7BVA82OYpjxP1b0USsdqCqfn_Y

server.port=8081